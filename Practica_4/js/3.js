// Sea el vector con los elementos [1, 3, 5, 7, 9, 10] . Recorrerlo y mostrar por la consola el doble de cada elemento del vector.

let vector = [1,3,5,7,9,10];
console.info("eje 1")
for (let i = 0 ; i < vector.length ; i++) {
        console.info(vector[i]*2);
    
}