const personas = [
    {'name': 'lucas', 'year':35},
    {'name': 'nicolas', 'year':25},
    {'name': 'lucas', 'year':50},
]

let result = personas.map(persona => ({nacimiento : 2024-persona.year, name : persona.name,}))

console.log(result);